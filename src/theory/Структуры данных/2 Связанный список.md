```   
      В связанном списке элементы распределяются в произвольных местах памяти, при этом в одном
    элементе хранится адрес следующего элемента. 
    
      В односвязном списке хранится ссылка на следующий элемент или null, если следующего элемента
    нет. В двусвязном хранится 2 ссылки, на предыдущий и следующий элементы списка.
```
